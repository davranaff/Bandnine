[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N"]
ignore = ["E501", "B008", "B009"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["app/tests"]
